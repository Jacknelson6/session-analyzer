# assets

Images referenced by the project README.

- `logo.png` — the project logo, shown at the top of `../README.md`.
- `how-it-works.png` — the usage diagram, shown in the "How it works" section.

After saving each file here, uncomment the matching image line in `../README.md`.
