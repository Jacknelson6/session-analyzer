# ledger (benchmark fixture)

A tiny expense-ledger library used as a fixed target for the session-analyzer
A/B benchmark. Tasks operate on a throwaway copy of this directory.

Run the tests: `python3 -m unittest discover -s tests -t . -q`
