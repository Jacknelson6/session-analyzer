# Verify before you finish

This task has a definition of done in README.md, and it includes edge cases that
a happy-path implementation will miss.

Before you respond that you are done:

1. Re-read the definition of done in README.md.
2. Write unittest tests that cover it, including edge cases and error cases.
3. Run them with `python3`. Fix the code (never weaken the tests) until they pass.

Do not finish until your own tests are green.
